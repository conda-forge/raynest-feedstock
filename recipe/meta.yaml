{% set name = "raynest" %}
{% set version = "1.1.2" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/raynest-{{ version }}.tar.gz
  sha256: 3d88564bbaa965b0826ce9a98f748499c893d0497e91ddafc91a5c4eaa713316

build:
  script: {{ PYTHON }} -m pip install . -vv
  number: 0
  # ray is not available on OSX yet
  skip: true  # [osx]

requirements:
  build:
    - {{ compiler('c') }}
    - {{ stdlib("c") }}
  host:
    - cython
    - numpy
    - pip
    - python
    - setuptools
    - setuptools-scm
  run:
    - python
    - numpy
    - scipy
    - corner
    - tqdm
    - matplotlib-base
    - ray-default
    - dill
    - h5py

test:
  imports:
    - raynest
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/wdpozzo/raynest
  summary: 'raynest: Parallel nested sampling based on ray'
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - xhochy
